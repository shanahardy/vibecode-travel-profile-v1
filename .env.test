# Test Environment Variables
# These are loaded during test execution
# The DATABASE_URL is required to prevent initialization errors
# but the actual database is mocked - no real connection is made

DATABASE_URL=postgresql://test:test@localhost:5432/test
SESSION_SECRET=test-session-secret-key-for-testing-only
NODE_ENV=test

# Stripe (mocked in tests)
STRIPE_SECRET_KEY=sk_test_mock_key_for_testing
STRIPE_WEBHOOK_SECRET=whsec_test_mock_webhook_secret
STRIPE_PRICE_ID_PRO=price_test_mock_price_id
VITE_STRIPE_PUBLIC_KEY=pk_test_mock_public_key

# SendGrid (mocked in tests)
SENDGRID_API_KEY=SG.test_api_key_for_testing
SENDGRID_FROM=test@example.com

# PostHog (mocked in tests)
POSTHOG_API_KEY=test_posthog_api_key
POSTHOG_HOST=https://app.posthog.com

# OpenAI (mocked in tests)
OPENAI_API_KEY=sk-test-openai-key
OPENAI_CHATKIT_WORKFLOW_ID=test-workflow-id

# Frontend URL
FRONTEND_URL=http://localhost:5000
